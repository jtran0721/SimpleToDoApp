pipeline {
    agent ('AWS-Build-Agents') 
    stages {
        stage('Test Build') {
            steps {
                echo 'Hello, Test'
            }
        }
        stage('Build') {
            steps {
                echo 'Hello, Build'
            }
        }
    }
}